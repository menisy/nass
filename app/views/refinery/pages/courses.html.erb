
<div class="row no-mrg">
  <div class="col-sm-9 courses">

    <%= raw(@page.content_for(:courses)) %>

  </div>
  <div class="col-sm-3">
    <span class="green-title">Schools</span>
    <div class="horz-divider">
    </div>
    <div class="schools">
      <%= raw(@page.content_for(:schools)) %>
    </div>

    <div class="horz-divider">
    </div>
    <span class="green-title">Contact Us</span>
    <div class="horz-divider">
    </div>
    <%= raw(@contact.content) %>
    <div class="horz-divider">
    </div>
    <%= render partial: 'shared/media_links' %>
    <%= render partial: 'shared/video' %>
  </div>

</div>
