<ul>
  <% if ::Refinery::Companies::Admin::JobRolesController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.companies_admin_job_roles_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_companies_admin_job_role_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? && ::Refinery::Companies::Admin::JobRolesController.sortable? && ::Refinery::Companies::JobRole.many? %>
  <li>
    <%= link_to t('.reorder', :what => "Job Roles"),
                 refinery.companies_admin_job_roles_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Job Roles"),
                 refinery.companies_admin_job_roles_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
