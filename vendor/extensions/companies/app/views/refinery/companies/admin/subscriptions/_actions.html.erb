<ul>
  <% if ::Refinery::Companies::Admin::SubscriptionsController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.companies_admin_subscriptions_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_companies_admin_subscription_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? && ::Refinery::Companies::Admin::SubscriptionsController.sortable? && ::Refinery::Companies::Subscription.many? %>
  <li>
    <%= link_to t('.reorder', :what => "Subscriptions"),
                 refinery.companies_admin_subscriptions_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Subscriptions"),
                 refinery.companies_admin_subscriptions_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
