<%= render 'refinery/admin/search_header', :url => refinery.companies_admin_education_levels_path %>
<div class='pagination_container'>
  <% if @education_levels.any? %>
    <%= render 'education_levels' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
