<section id='records'>
  <%= render 'records' %>
</section>
<aside id='actions'>
  <%= render partial: 'actions', locals: {export: true} %>
</aside>
<%= render '/refinery/admin/make_sortable', :tree => false if !searching? and ::Refinery::Companies::Admin::CompaniesController.sortable? and ::Refinery::Companies::Company.many? %>
