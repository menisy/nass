<li class='clearfix record <%= cycle("on", "on-hover") %>' id="<%= dom_id(city) -%>">
  <span class='title'>
    <%= city.name %>

    <% if Refinery::I18n.frontend_locales.many? %>
      <span class='preview'>
        <% city.translations.each do |translation| %>
          <% if translation.name.present? %>
            <%= link_to refinery_icon_tag("flags/#{translation.locale}.png", :size => '16x11'),
                         refinery.edit_companies_admin_city_path(city, :switch_locale => translation.locale),
                         :class => 'locale' %>
          <% end %>
        <% end %>
      </span>
    <% end %>

  </span>
  <span class='actions'>
    
    <%= link_to refinery_icon_tag("application_go.png"), refinery.companies_city_path(city),
        :title => t('.view_live_html'),
        :target => "_blank" %>
    
    <%= link_to refinery_icon_tag("application_edit.png"), refinery.edit_companies_admin_city_path(city),
         :title => t('.edit') %>
    <%= link_to refinery_icon_tag("delete.png"), refinery.companies_admin_city_path(city),
        :class => "cancel confirm-delete",
        :title => t('.delete'),
        :confirm => t('message', :scope => 'refinery.admin.delete', :title => city.name),
        :method => :delete %>
  </span>
</li>
