<%= render 'refinery/admin/search_header', :url => refinery.companies_admin_job_types_path %>
<div class='pagination_container'>
  <% if @job_types.any? %>
    <%= render 'job_types' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
