<%= render 'refinery/admin/search_header', :url => refinery.companies_admin_career_levels_path %>
<div class='pagination_container'>
  <% if @career_levels.any? %>
    <%= render 'career_levels' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
