<ul>
  <% if ::Refinery::Companies::Admin::JobsController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.companies_admin_jobs_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_companies_admin_job_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? && ::Refinery::Companies::Admin::JobsController.sortable? && ::Refinery::Companies::Job.many? %>
  <li>
    <%= link_to t('.reorder', :what => "Jobs"),
                 refinery.companies_admin_jobs_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Jobs"),
                 refinery.companies_admin_jobs_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
