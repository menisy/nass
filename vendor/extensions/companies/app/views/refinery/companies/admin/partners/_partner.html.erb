<li class='clearfix record <%= cycle("on", "on-hover") %>' id="<%= dom_id(partner) -%>">
  <span class='title'>
    <%= partner.name %>

    <% if Refinery::I18n.frontend_locales.many? %>
      <span class='preview'>
        <% partner.translations.each do |translation| %>
          <% if translation.name.present? %>
            <%= link_to refinery_icon_tag("flags/#{translation.locale}.png", :size => '16x11'),
                         refinery.edit_companies_admin_partner_path(partner, :switch_locale => translation.locale),
                         :class => 'locale' %>
          <% end %>
        <% end %>
      </span>
    <% end %>

  </span>
  <span class='actions'>
    
    <%= link_to refinery_icon_tag("application_edit.png"), refinery.edit_companies_admin_partner_path(partner),
         :title => t('.edit') %>
    <%= link_to refinery_icon_tag("delete.png"), refinery.companies_admin_partner_path(partner),
        :class => "cancel confirm-delete",
        :title => t('.delete'),
        :confirm => t('message', :scope => 'refinery.admin.delete', :title => partner.name),
        :method => :delete %>
  </span>
</li>
