<% content_for :body do %>
  <ul id="areas">
    <% @areas.each do |area| %>
      <li>
        <%= link_to area.name, refinery.companies_area_path(area) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
