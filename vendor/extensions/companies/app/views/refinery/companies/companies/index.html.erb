<% content_for :body do %>
  <ul id="companies">
    <% @companies.each do |company| %>
      <li>
        <%= link_to company.company_name, refinery.companies_company_path(company) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
