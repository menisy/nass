<%= render 'refinery/admin/search_header', :url => refinery.personal_infos_admin_personal_infos_path %>
<div class='pagination_container'>
  <% if @personal_infos.any? %>
    <%= render 'personal_infos' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
