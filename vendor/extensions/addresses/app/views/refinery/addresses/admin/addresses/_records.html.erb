<%= render 'refinery/admin/search_header', :url => refinery.addresses_admin_addresses_path %>
<div class='pagination_container'>
  <% if @addresses.any? %>
    <%= render 'addresses' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
