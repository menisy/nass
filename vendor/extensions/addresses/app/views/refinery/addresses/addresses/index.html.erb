<% content_for :body do %>
  <ul id="addresses">
    <% @addresses.each do |address| %>
      <li>
        <%= link_to address.name, refinery.addresses_address_path(address) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
