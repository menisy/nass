<%= render 'refinery/admin/search_header', :url => refinery.schools_admin_course_classes_path %>
<div class='pagination_container'>
  <% if @course_classes.any? %>
    <%= render 'course_classes' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
