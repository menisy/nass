<%= render 'refinery/admin/search_header', :url => refinery.home_photos_admin_home_photos_path %>
<div class='pagination_container'>
  <% if @home_photos.any? %>
    <%= render 'home_photos' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
